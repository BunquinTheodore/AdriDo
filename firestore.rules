rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single user mode - allow access to default-user data only
    match /users/default-user/{document=**} {
      allow read, write: if true;
    }
  }
}
